@model IEnumerable<Markup>
@foreach (var item in Model)
{
        <tr>
            <td style="cursor:pointer;" onclick="GetDocuments(@item.Id)">


            @Html.Raw(item.Body)

            </td>
        </tr>
}

